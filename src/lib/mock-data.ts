import type { FileStatus, MonitoredPath } from '@/types';

// This file is now deprecated as data is managed in database.json
// It is kept for reference or future use if needed.

export const initialFileStatuses: FileStatus[] = [];

export const initialMonitoredPaths: MonitoredPath[] = [];

export const initialMonitoredExtensions: string[] = [];
